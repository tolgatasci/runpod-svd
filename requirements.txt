# RunPod SVD-XT Requirements
# Fixed versions to avoid torch.xpu compatibility issue

# RunPod SDK
runpod>=1.7.0

# Core ML Framework (matches base image)
torch>=2.1.0,<2.3.0
torchvision>=0.16.0

# Diffusers - pinned to avoid xpu error
diffusers==0.25.1
transformers>=4.36.0,<4.42.0
accelerate>=0.25.0,<0.32.0
safetensors>=0.4.0

# HuggingFace Hub
huggingface-hub>=0.20.0,<0.24.0

# Image Processing
Pillow>=10.0.0
numpy>=1.24.0

# Video Export
imageio>=2.33.0
imageio-ffmpeg>=0.4.9

# HTTP Requests
requests>=2.31.0

# CLIP dependencies
ftfy>=6.1.0
regex>=2023.0.0
